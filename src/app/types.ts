export type Menu = {
    id: string;
    path: string;
    name: string;
}

export type ServiceItem = {
    id: string;
    title: string;
    subtitle?: string;
    price?: string;
    image: string;
}